# Traduzioni

Il plugin è scritto in italiano e usa il text domain `db-avvisi`.

Per tradurlo in un'altra lingua genera qui i file `db-avvisi-<locale>.po` / `.mo`
(es. `db-avvisi-en_US.po`) a partire dalle stringhe marcate con `__()` ed `esc_html__()`.
